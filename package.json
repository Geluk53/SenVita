{
  "name": "senvita",
  "version": "1.0.0",
  "scripts": {
    "start-all": "concurrently \"cd server && node --no-deprecation server.js\" \"cd client && npx vite --port 5174 --open\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
